# 文献格式化修改说明

## 修改目标
将文献格式化逻辑从直接使用 `ProjectApplicationLiteratureDTO` 改为通过OpenFeign远程调用获取 `MongoLiterature` 数据，并按照标准格式进行拼接。

## 最终文献格式
```
[序号] 作者1, 作者2, 作者3, 等. 标题[J]. 期刊名, 年份, 卷(期): 页码. DOI:doi值.
```

## 修改内容

### 1. 添加必要的导入（ProgressDisplayAgent.java）
- 导入 `com.faers.agent.pojo.MongoLiterature`
- 导入 `com.faers.agent.utils.PaperUtils`

### 2. 修改 `formatLiteratureReference` 方法签名
**修改前：**
```java
private String formatLiteratureReference(ProjectApplicationLiteratureDTO literature, int number)
```

**修改后：**
```java
private String formatLiteratureReference(String literatureId, int number)
```

### 3. 修改 `formatLiteratureReference` 方法实现
- 使用 `PaperUtils.paper(literatureId)` 通过OpenFeign远程调用获取完整的 `MongoLiterature` 数据
- 参考 `analylog.text` 中的格式化逻辑进行文献拼接
- 处理作者列表：超过3个作者时只显示前3个，并根据语言添加"等"或"et al"
- 按照标准格式拼接：作者、标题[J]、期刊、年份、卷(期)、页码、DOI

### 4. 修改 `getLiterature` 方法中的调用
**修改前：**
```java
String formattedReference = formatLiteratureReference(literature, literatureNumber);
```

**修改后：**
```java
String formattedReference = formatLiteratureReference(literature.getId(), literatureNumber);
```

## 格式化规则详解

1. **作者处理**
   - 作者之间用逗号加空格分隔：`作者1, 作者2, 作者3`
   - 超过3个作者时只显示前3个，并加上"等"（中文）或"et al"（英文）
   - 最后一个作者后加句号和空格

2. **标题**
   - 去除方括号
   - 标题后加 `[J]. `

3. **期刊**
   - 期刊名后加逗号和空格

4. **年份**
   - 年份后加逗号和空格
   - 过滤 "unkn" 值

5. **卷和期**
   - 格式：`卷(期): `
   - 示例：`25(3): `

6. **页码**
   - 去除方括号
   - 页码后加句号和空格

7. **DOI**
   - 格式：`DOI:doi值.`（冒号后无空格）
   - 支持多个DOI

## 错误处理
- 当远程调用返回 `null` 时，返回提示信息
- 当格式化过程中发生异常时，返回包含错误信息的提示
- 记录详细的错误日志便于排查问题

## 示例输出

**中文文献：**
```
[1] 张三, 李四, 王五, 等. 基于深度学习的医学图像分割研究[J]. 计算机学报, 2023, 46(3): 456-468. DOI:10.1234/example.2023.03.001.
```

**英文文献：**
```
[2] Smith J, Johnson A, Brown C, et al. Deep learning for medical image segmentation[J]. Nature Medicine, 2024, 30(2): 123-135. DOI:10.1038/s41591-024-00001-1.
```

## 相关文件
- 修改文件：`src/main/java/com/faers/agent/agent/impl/ProgressDisplayAgent.java`
- 参考文件：`analylog.text`
- Feign接口：`src/main/java/com/faers/agent/feign/FineScreenFeign.java`
- 工具类：`src/main/java/com/faers/agent/utils/PaperUtils.java`
- 实体类：`src/main/java/com/faers/agent/pojo/MongoLiterature.java`（未修改）




